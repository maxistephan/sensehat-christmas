#!/usr/bin/make -f

export DH_VERBOSE=1

export PYBUILD_NAME=christmaspi

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_installinit:
	dh_installinit --name=christmaspi

override_dh_installsystemd:
	dh_installsystemd --name=christmaspi
